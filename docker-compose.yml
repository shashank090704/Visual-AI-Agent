version: '3.8'

services:
  mongodb:
    image: mongo:7.0
    container_name: visual_ai_mongo
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: visual_ai_agent
    volumes:
      - mongo_data:/data/db
    restart: always

volumes:
  mongo_data:
